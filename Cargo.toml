[package]
name = "claude-hook-advisor"
version = "0.1.0"
edition = "2021"
description = "A Claude Code hook that suggests better command alternatives based on project preferences"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
clap = { version = "4.0", features = ["derive"] }
regex = "1.0"
anyhow = "1.0"
